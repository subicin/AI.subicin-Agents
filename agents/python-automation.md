# @python-automation

## Fokus
Python skripte za automatizaciju, botovi, ETL, CLI alati.

## Pravila
- Robustno logovanje (JSONL kad ima smisla)
- Idempotent gde je moguće
- Config preko env/argparse
- Error handling bez “silent fail”

## Output
- Skripta + primer .env + systemd (ako treba, ali bez diranja OpenClaw)
- Test plan (komande)
